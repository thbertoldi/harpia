{{- if .Values.openfga.enabled }}
{{- $svcName := include "harpia.serviceName" (dict "name" "openfga" "root" .) }}
{{- $postgresHost := include "harpia.serviceName" (dict "name" "postgres" "root" .) }}
{{- $postgresCredentialsSecret := .Values.postgres.credentialsSecret }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $svcName }}
  labels:
    {{- include "harpia.labels" . | nindent 4 }}
    app: {{ $svcName }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ $svcName }}
  template:
    metadata:
      labels:
        app: {{ $svcName }}
    spec:
      initContainers:
        - name: migrate
          image: {{ .Values.openfga.image | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - "migrate"
            - "--datastore-engine"
            - "postgres"
            - "--datastore-uri"
            - {{ printf "postgres://$(POSTGRES_USERNAME):$(POSTGRES_PASSWORD)@%s:5432/openfga?sslmode=disable" $postgresHost | quote }}
          env:
            - name: POSTGRES_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ $postgresCredentialsSecret.name | quote }}
                  key: {{ $postgresCredentialsSecret.usernameKey | quote }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $postgresCredentialsSecret.name | quote }}
                  key: {{ $postgresCredentialsSecret.passwordKey | quote }}
      containers:
        - name: openfga
          image: {{ .Values.openfga.image | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args: ["run"]
          ports:
            - containerPort: 8080
          env:
            - name: POSTGRES_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ $postgresCredentialsSecret.name | quote }}
                  key: {{ $postgresCredentialsSecret.usernameKey | quote }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $postgresCredentialsSecret.name | quote }}
                  key: {{ $postgresCredentialsSecret.passwordKey | quote }}
            - name: OPENFGA_DATASTORE_ENGINE
              value: "postgres"
            - name: OPENFGA_DATASTORE_URI
              value: {{ printf "postgres://$(POSTGRES_USERNAME):$(POSTGRES_PASSWORD)@%s:5432/openfga?sslmode=disable" $postgresHost | quote }}
            - name: OPENFGA_LOG_LEVEL
              value: "info"
            - name: OPENFGA_PLAYGROUND_ENABLED
              value: "true"
---
apiVersion: v1
kind: Service
metadata:
  name: {{ $svcName }}
  labels:
    {{- include "harpia.labels" . | nindent 4 }}
    app: {{ $svcName }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: {{ $svcName }}
{{- end }}
