# Harpia Dev — OpenFGA authorization
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: openfga
  labels:
    app: openfga
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openfga
  template:
    metadata:
      labels:
        app: openfga
    spec:
      initContainers:
        - name: migrate
          image: docker.io/openfga/openfga:v1.8.0
          args:
            - "migrate"
            - "--datastore-engine"
            - "postgres"
            - "--datastore-uri"
            - "postgres://$(POSTGRES_USERNAME):$(POSTGRES_PASSWORD)@postgres:5432/openfga?sslmode=disable"
          env:
            - name: POSTGRES_USERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: password
      containers:
        - name: openfga
          image: docker.io/openfga/openfga:v1.8.0
          args: ["run"]
          ports:
            - containerPort: 8080
          env:
            - name: POSTGRES_USERNAME
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: password
            - name: OPENFGA_DATASTORE_ENGINE
              value: postgres
            - name: OPENFGA_DATASTORE_URI
              value: "postgres://$(POSTGRES_USERNAME):$(POSTGRES_PASSWORD)@postgres:5432/openfga?sslmode=disable"
            - name: OPENFGA_LOG_LEVEL
              value: info
            - name: OPENFGA_PLAYGROUND_ENABLED
              value: "true"
---
apiVersion: v1
kind: Service
metadata:
  name: openfga
  labels:
    app: openfga
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: openfga
