## 1. Slice 1 — Contracts + pure projection

- [x] 1.1 Clarify the execution-focus boundary in `docs/architecture/harpia-platform.md` §9 and reconcile evidence in `openspec/changes/composable-linkedin-execution/tasks.md`: mark completed implementation tasks 2.2, 2.3, and 2.4 as checked without changing their historical acceptance text. **Depends on:** none. **Acceptance:** Given a reader of Constitution §9, when they inspect execution focus, then “active” means focus over an exact/selected PlanExecution rather than singleton Thread state; given the completed LinkedIn change task list, when they inspect its contract/persistence/artifact tasks, then 2.2–2.4 agree with the completed implementation evidence. **Focused verification:** `git diff --check -- docs/architecture/harpia-platform.md openspec/changes/composable-linkedin-execution/tasks.md`; `openspec validate execution-driven-conversation --strict --no-interactive`.

- [x] 1.2 Add the breaking conversation/execution contracts in `proto/harpia/chat/v1/chat.proto` and `proto/harpia/plans/v1/plans.proto`: add `THREAD_MESSAGE_KIND_EXECUTION_PROMPT = 29`, typed `ExecutionPromptPayload` (stable IDs/pointers/counts/ArtifactRef/action IDs only, no backend-authored labels), and `PlanExecution.plan_template_snapshot = 12`. Regenerate checked-in clients with `mise run buf-generate`. **Depends on:** 1.1. **Acceptance:** Given an execution prompt serialized in any generated consumer, when it represents a focused interaction, then it carries configuration/execution/step/request/artifact-version identity and no presentation labels; given a PlanExecution API response, when its snapshot contains a template, then the proto exposes the exact frozen template. **Focused verification:** `mise run buf-generate`; `cd proto && buf lint`; `cd control-plane && go test ./internal/chat/... ./internal/plans/...`; `cd frontend && bun run check` (record the documented 12-error baseline separately and fail on any new error).

- [x] 1.3 Replace `DeriveState` in `control-plane/internal/planassistant/{state.go,state_test.go,controller.go}` and its call sites in `control-plane/internal/plans/{configuration_selection.go,handler_test.go}` with `DeriveConfigurationState(template, configuration)`. Remove the unused messages parameter and `StateSaved`; return only DRAFT configuration states and move RUNNABLE/SCHEDULED/DISABLED/ARCHIVED behavior out of this reducer. **Depends on:** 1.2. **Acceptance:** Given a DRAFT configuration, when bindings, overseers, and policies are incomplete, then the renamed reducer returns the same appropriate configuration state; given a non-DRAFT configuration, when the reducer is called, then it cannot produce a saved/landing execution state. **Focused verification:** `cd control-plane && go test ./internal/planassistant/... ./internal/plans/...`.

- [x] 1.4 Add pure `ConversationTurn` and `ExecutionAssistantView` derivation in `control-plane/internal/planassistant/{conversation_turn.go,conversation_turn_test.go}`. Model all approved `ConversationTurnKind` values, exact-target/display-fallback resolution, terminal-turn preservation, execution prompt fingerprint inputs, frozen template + `active_step_keys` ordering, latest pinned artifact selection, and the ≤1 pending-interaction fail-safe. Keep the pure package free of database, Connect, Temporal, provider, and frontend dependencies. **Depends on:** 1.2, 1.3. **Acceptance:** Given DRAFT/no target, RUNNABLE/no target, a queued/running/exact interaction execution, and each terminal execution, when the pure driver derives a turn, then it returns CONFIGURING, the correct ready state, the correct execution state, and a preserved terminal turn respectively; given two pending interaction pointers or incompatible durable state, then it returns `EXECUTION_NEEDS_ATTENTION` and no actionable pointer; given an event-targeted execution and a newer run, then it keeps the exact target. **Focused verification:** `cd control-plane && go test ./internal/planassistant/...`.

- [x] 1.5 Run Slice 1 contract/projection gates after tasks 1.2–1.4. **Depends on:** 1.2, 1.3, 1.4. **Acceptance:** Given the Slice 1 tree, when contracts and pure projection tests run, then proto lint and affected Go tests pass, generated frontend contracts introduce no type errors beyond the documented 12-error baseline, and DB-backed acceptance remains executable. **Verification:** `openspec validate execution-driven-conversation --strict --no-interactive`; `cd proto && buf lint`; `cd control-plane && go test ./internal/planassistant/... ./internal/chat/... ./internal/plans/...`; `cd frontend && bun run lint`; `cd frontend && bun run check` (report new errors separately from the 12-error baseline); `mise run acceptance`.

## 2. Slice 2 — Durable projection + event driver + signal correctness

- [ ] 2.1 Add tenant-safe execution conversation projection reads in `control-plane/internal/plans/{repository.go,model.go,runtime.go,runtime_test.go}`, `control-plane/internal/plans/{reviews.go,approvals_handler.go,reviews_handler.go}`, and focused new tests `control-plane/internal/plans/execution_conversation_projection_test.go`. Reads SHALL load an exact execution or the selected configuration's latest non-terminal execution for display only, plus its StepExecutions, frozen snapshot/template, pending elicitation/review/approval, and pinned ArtifactRefs under the existing RLS boundary. **Depends on:** 1.4. **Acceptance:** Given two tenants or two concurrent same-step executions, when the projection reads one execution/request, then it returns only rows belonging to the caller tenant and exact execution/StepExecution; given no exact target, then a latest non-terminal execution is available only as display projection and cannot be returned as an action target. **Focused verification:** `cd control-plane && go test ./internal/plans/...`.

- [ ] 2.2 Fix the review decision double-write in `control-plane/internal/plans/{reviews_handler.go,reviews_e2e_test.go}` and `control-plane/internal/workflow/{plans.go,worker.go,plans_test.go}`. `RespondToReviewRequest` SHALL authorize/load the pending request and send the exact `ReviewDecisionSignal`; only `ResolveReviewRequestActivity` SHALL call `MarkReviewDecided` and emit `REVIEW_DECIDED`. Preserve idempotent duplicate-signal validation by both `step_execution_id` and `review_request_id`. **Depends on:** 2.1; MUST land before 2.3 consumes review-decided events. **Acceptance:** Given an authorized accept/revise request, when the handler runs, then it does not terminally update the review before signaling; when the workflow resolves the valid signal, then it persists the decision once and emits `REVIEW_DECIDED` once; given a duplicate or mismatched signal, then it cannot settle a different request/execution. **Focused verification:** `cd control-plane && go test ./internal/plans/... ./internal/workflow/...`.

- [ ] 2.3 Wire execution-transition notifications to the conversation driver in `control-plane/internal/planassistant/{controller.go,execution_driver.go,execution_driver_test.go}`, `control-plane/internal/chat/{messages.go,messages_test.go}`, `control-plane/internal/plans/{runtime.go,runtime_test.go}`, and `control-plane/internal/workflow/{plans.go,plans_test.go}`. Emit and deduplicate `EXECUTION_PROMPT` by the approved fingerprint after execution, interaction, artifact, and terminal transitions; preserve `ASSISTANT_PROMPT` as the configuration-only unanswered prompt. **Depends on:** 2.1, 2.2. **Acceptance:** Given duplicate `RUN_*`, `STEP_*`, interaction-raised, or interaction-decided notifications, when the driver is invoked repeatedly, then at most one equivalent EXECUTION_PROMPT is appended; given a pending review, then the prompt points to its exact execution, StepExecution, review request, and subject artifact version without a backend-authored label. **Focused verification:** `cd control-plane && go test ./internal/planassistant/... ./internal/chat/... ./internal/plans/... ./internal/workflow/...`.

- [ ] 2.4 Prove event-to-command-to-signal routing in `control-plane/internal/plans/{elicitations_e2e_test.go,reviews_e2e_test.go,approvals_handler_test.go}` and new `control-plane/internal/plans/execution_action_routing_test.go`. Cover Elicitation answer → `ElicitationResponseSignal`, Review accept/revise → `ReviewDecisionSignal`, Approval approve/reject → `ApprovalDecisionSignal`, and `STEP_FAILED` Retry/Run-again/Reconfigure eligibility. Browser-facing command inputs SHALL remain request/action IDs only; all execution/step identity derives server-side. **Depends on:** 2.2, 2.3. **Acceptance:** Given two concurrent executions with identical step keys, when a request ID from execution A is acted on, then the emitted signal targets execution A's StepExecution only; given an unauthorized card viewer or a supplied replacement identity, then the command is rejected; given a failure, then Retry uses the failed snapshot/upstream outputs, Run-again creates a fresh snapshot, and Reconfigure is unavailable while a non-terminal focus exists. **Focused verification:** `cd control-plane && go test ./internal/plans/... ./internal/workflow/...`.

- [ ] 2.5 Run Slice 2 durable/event/signal gates after tasks 2.1–2.4. **Depends on:** 2.1, 2.2, 2.3, 2.4. **Acceptance:** Given the Slice 2 tree, when full touched-backend and generated-contract gates run, then execution prompts are deduplicated, review has one persistence owner, and concurrent routing tests pass without new frontend type errors. **Verification:** `openspec validate execution-driven-conversation --strict --no-interactive`; `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd frontend && bun run lint`; `cd frontend && bun run check` (report new errors separately from the 12-error baseline); `mise run acceptance`.

## 3. Slice 3 — Execution conversation UI

- [ ] 3.1 Fold `EXECUTION_PROMPT` and frozen execution snapshots into `frontend/src/lib/chat/{types.ts,types.test.ts}`, `frontend/src/lib/plans/{thread.ts,thread.test.ts,execution-view.ts,execution-view.test.ts}`, and regenerated `frontend/src/lib/gen/harpia/{chat/v1/chat_pb.ts,plans/v1/plans_pb.ts}`. Derive titles/order/active rows from `planTemplateSnapshot` plus `activeStepKeys`, retain complete version-pinned `STEP_BOUND` refs, and fail safe for contradictory interaction projection. **Depends on:** 1.2, 2.2. **Acceptance:** Given a later template edit, when a historical or in-flight execution card is folded, then its order/titles remain from the frozen snapshot; given two pending interactions, then the UI renders a localized needs-attention state and does not choose an action; given a prompt from another execution, then it is not folded into the focused execution. **Focused verification:** `cd frontend && bunx vitest run src/lib/chat/types.test.ts src/lib/plans/thread.test.ts src/lib/plans/execution-view.test.ts`; `cd frontend && bun run check` (report new errors separately from the 12-error baseline).

- [ ] 3.2 Add the execution prompt and actionable checkpoint surfaces in `frontend/src/lib/components/thread/{ExecutionPromptCard.svelte,ExecutionPromptCard.test.ts,ElicitationRefCard.svelte,ReviewRefCard.svelte,ApprovalRefCard.svelte,PlanExecutionCard.svelte,ThreadMessage.svelte}`. Wire Preview, Answer, Accept/Revise, Approve/Reject, Retry, Run-again, and Reconfigure to existing generated command clients by projected request/action ID only. Do not expose `SignalWorkflow`, execution substitution, or mutable artifact preview selection. Add every new user-facing string, aria label, status, and action in both `frontend/src/lib/i18n/en.json` and `frontend/src/lib/i18n/pt-BR.json`; use only Harpy Eclipse tokens and opacity/transform/layout motion. **Depends on:** 3.1, 2.4. **Acceptance:** Given each projected pending interaction, when its card is rendered, then its action calls the matching command with its request/action ID and Preview opens the pinned artifact version; given a terminal failure, then Retry and Run-again are distinct actions and Reconfigure appears only when no non-terminal execution is focused; given either supported locale, then no new visible label or aria string is raw copy. **Focused verification:** `cd frontend && bunx vitest run src/lib/components/thread/ExecutionPromptCard.test.ts src/lib/components/thread/PlanExecutionCard.test.ts src/lib/components/thread`; `cd frontend && bunx vitest run src/lib/i18n`; `cd frontend && bun run lint`.

- [ ] 3.3 Scope primary and off-focus rendering in `frontend/src/routes/chat/[threadId]/+page.svelte`, `frontend/src/lib/components/{PlanThreadTopBar.svelte,thread/ConversationalWorkspace.svelte}`, and tests `frontend/src/routes/chat/[threadId]/page.test.ts` (create if absent) plus `frontend/src/lib/components/thread/ConversationalWorkspace.test.ts` (create if absent). Make the selected PlanConfiguration + exact execution primary and show other configurations/executions as compact attention without changing focus or authorizing their actions. **Depends on:** 3.1, 3.2. **Acceptance:** Given two configurations with concurrent executions, when a user selects configuration A, then A's targeted execution is primary and B is compact attention; when a B action is selected, then navigation supplies B's exact identity before command invocation rather than borrowing A's latest execution. **Focused verification:** `cd frontend && bunx vitest run src/routes/chat/[threadId]/page.test.ts src/lib/components/thread/ConversationalWorkspace.test.ts src/lib/plans/execution-view.test.ts`; `cd frontend && bun run check` (report new errors separately from the 12-error baseline).

- [ ] 3.4 Run Slice 3 UI gates after tasks 3.1–3.3. **Depends on:** 3.1, 3.2, 3.3. **Acceptance:** Given the Slice 3 tree, when UI tests and whole frontend gates run, then execution prompts, pinned previews, exact command routing, localization parity, and focus/off-focus rendering pass without a new type-check error. **Verification:** `openspec validate execution-driven-conversation --strict --no-interactive`; `cd frontend && bunx vitest run src/lib/chat/types.test.ts src/lib/plans/execution-view.test.ts src/lib/components/thread src/lib/i18n`; `cd frontend && bun run lint`; `cd frontend && bun run check` (report new errors separately from the 12-error baseline); `mise run acceptance`.

## 4. Slice 4 — Boundary, concurrency acceptance, delivery gates

- [ ] 4.1 Add boundary and snapshot acceptance coverage in `control-plane/internal/plans/{execution_conversation_e2e_test.go,handler_test.go}` and `control-plane/internal/workflow/{plans_test.go,plans_acceptance_test.go}` (create `plans_acceptance_test.go` if absent). Prove DRAFT → save → RUNNABLE → user Run → fresh snapshot → execution conversation; prove Run-again uses a fresh current configuration snapshot, Retry uses the failed snapshot with upstream reuse, and strict Reconfigure cannot mutate an in-flight snapshot, demote RUNNABLE, or clone a PlanConfiguration. **Depends on:** 2.4, 3.3. **Acceptance:** Given a saved DRAFT plan, when the user runs it, then a pending execution with frozen configuration/template snapshot is created and its execution prompt becomes conversational focus; given later configuration edits, then they affect only a Run-again execution and never a retry/in-flight execution. **Focused verification:** `cd control-plane && go test ./internal/plans/... ./internal/workflow/...`; `mise run acceptance`.

- [ ] 4.2 Add end-to-end cross-execution isolation coverage in `control-plane/internal/plans/execution_conversation_e2e_test.go`, `control-plane/internal/workflow/plans_test.go`, and `frontend/src/lib/plans/execution-view.test.ts`: use two PlanConfigurations in one Thread and concurrent executions with identical step keys; target elicitation, review, and approval decisions by request id; and assert no decision crosses configuration, execution, StepExecution, or artifact version. **Depends on:** 4.1. **Acceptance:** Given concurrent `publish` steps in two executions, when an accept/revise/approve/answer decision is made for one request, then only that request's execution proceeds and the other remains pending; given stale/current Artifact changes, then each preview retains its exact pinned version. **Focused verification:** `cd control-plane && go test ./internal/plans/... ./internal/workflow/...`; `cd frontend && bunx vitest run src/lib/plans/execution-view.test.ts`; `mise run acceptance`.

- [ ] 4.3 Run and record all final delivery gates; do not mark this task complete while any applicable linter or acceptance gate fails. **Depends on:** 4.2. **Acceptance:** Given the finished change, when validation and whole-surface gates run, then OpenSpec validates strictly, all linters pass, backend tests pass, Helm lint passes, acceptance passes, and frontend checking has no errors beyond the documented 12-error baseline (reported separately). **Verification:** `openspec validate execution-driven-conversation --strict --no-interactive`; `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint`; `cd frontend && bun run check`; `mise run helm-lint`; `mise run acceptance`.
