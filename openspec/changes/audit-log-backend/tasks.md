## 1. Persisted audit contract

- [ ] 1.1 Create `database/migrations/000018_audit_events.sql` and regenerate `database/migrations/atlas.sum`. Define the append-only tenant-scoped `audit_events` table, forced RLS policy using `current_tenant_id()`, safe actor/subject/diff/decision/trace/timestamp columns, and tenant-prefixed keyset, event-type, and actor indexes from `design.md`. **Depends on:** none. **Acceptance:** Given tenant A and tenant B database contexts, when an event is inserted or listed, then forced RLS permits only the matching tenant; given equal timestamps, when a next-page cursor is used, then the `(occurred_at, id)` tuple remains strictly ordered. **Focused verification:** `atlas migrate hash --dir file://database/migrations && atlas migrate validate --dir file://database/migrations` from the repository root.

- [ ] 1.2 Add `proto/harpia/audit/v1/audit.proto` with the read-only `harpia.audit.v1.AuditService.ListAuditEvents` contract: structured event/actor/subject/diff messages, validated initial event vocabulary, tenant/event-type/actor/date/subject/decision filters, bounded page size, and opaque keyset token. Run `mise run buf-generate` so generated bindings are committed under `control-plane/gen/harpia/audit/v1/`, `frontend/src/lib/gen/harpia/audit/v1/`, and `agent-runtime/src/harpia_agents/gen/harpia/audit/v1/`. **Depends on:** 1.1. **Acceptance:** Given a generated client, when it requests a valid filtered page, then it can read typed events and a next token; when it sends a malformed cursor, then the server contract supports `InvalidArgument`; no create/record RPC exists. **Focused verification:** `mise run buf-generate && (cd proto && buf lint)`.

## 2. Audit adapter, storage, and query API

- [ ] 2.1 Create `control-plane/internal/audit/model.go`, `repository.go`, `repository_test.go`, `cursor.go`, `cursor_test.go`, `handler.go`, and `handler_test.go`. Implement tenant-context-safe append/list storage, allowlisted diff redaction, opaque tuple cursor encode/decode, filter composition, protobuf mapping, page-size validation, and a `ListAuditEvents` handler that calls `identity.RequireTenant`. **Depends on:** 1.1 and 1.2. **Acceptance:** Given events from two tenants and every supported filter, when an authorized tenant lists pages, then only its matching events are returned newest-first without overlap; when a token is invalid, mismatches its filter scope, or requests another tenant, then the handler returns the specified Connect error and no data. **Focused verification:** `cd control-plane && go test ./internal/audit -run 'TestRepository|TestCursor|TestHandler'`.

- [ ] 2.2 Create `control-plane/internal/audit/recorder.go`, `writer.go`, `writer_test.go`, and `recorder_test.go`; add queue/batch/retry/flush configuration to `control-plane/internal/config/config.go` and its tests. The recorder must validate/redact a copied draft, enqueue without waiting for an insert, write batches through the tenant-safe repository, retry with bounded backoff, drain on orderly shutdown, and emit secret-free structured failure evidence on saturation/exhaustion. **Depends on:** 2.1. **Acceptance:** Given a delayed or failed repository, when an audited public request succeeds, then its response does not wait for the write; given shutdown, then queued events drain; given queue/batch failure, then the failure is observable without leaking a credential or blocking the operation. **Focused verification:** `cd control-plane && go test ./internal/audit -run 'TestRecorder|TestWriter'`.

- [ ] 2.3 Wire the generated AuditService handler, repository, recorder lifecycle, and orderly shutdown into the API process in `control-plane/cmd/api/main.go`. Place the audit interceptor after `identity.NewRequestContextInterceptor` and keep the audit package as an adapter rather than importing Connect/pgx into plan, executor, or workflow domain code. **Depends on:** 2.1 and 2.2. **Acceptance:** Given API startup with a database pool, when the audit route is registered, then it shares authenticated tenant context with public services; when API shutdown starts, then the writer is stopped only after its drain attempt. **Focused verification:** `cd control-plane && go test ./cmd/api ./internal/audit`.

## 3. Existing-operation recording paths

- [ ] 3.1 Create `control-plane/internal/audit/interceptor.go`, `interceptor_test.go`, and a table-driven existing-operation registry. Update `control-plane/internal/identity/context.go` and `middleware.go` only as needed to expose that a request used the enabled dev token without exposing the token. The interceptor must record one successful `authentication.dev_auth` event for that path and capture tenant, human actor, and trace information after identity resolution. **Depends on:** 2.2 and 2.3. **Acceptance:** Given an enabled dev token and a successful public request, when identity resolves it, then exactly one credential-free dev-auth event is queued; given normal bearer auth, an unsuccessful request, or dev auth disabled, then no dev-auth event is queued. **Focused verification:** `cd control-plane && go test ./internal/audit ./internal/identity -run 'TestAuditInterceptor|TestDevAuth'`.

- [ ] 3.2 Update `control-plane/internal/plans/handler.go`, `configuration_update.go`, and their focused tests to enrich the interceptor's request-scoped draft for existing PlanConfiguration creation and updates (including parameter values, status, SlotBinding, OverseerBinding, and PlanBehaviorPolicies) and PlanExecution creation. Add only safe, selected before/after values and typed PlanConfiguration/PlanExecution subjects. **Do not add a delete RPC or handler.** **Depends on:** 3.1. **Acceptance:** Given a `DRAFT → RUNNABLE` configuration update or an existing binding/policy/parameter mutation, when the RPC succeeds, then exactly one `plan_configuration.*` event contains the correct subject and safe diff; given plan execution creation, then one execution-created event is recorded; given a rejected mutation, then no successful event exists. **Focused verification:** `cd control-plane && go test ./internal/plans ./internal/audit -run 'Test.*Audit|TestCreatePlanExecution|TestUpdatePlanConfiguration'`.

- [ ] 3.3 Update `control-plane/internal/executors/handler.go` and `handler_test.go` to enrich audit drafts for the existing `CreateExecutorInstallation`, `UpdateExecutorInstallation`, and `DeleteExecutorInstallation` RPCs. Diff only safe display/enablement/connection state and configuration metadata; never serialize `config_json` credentials, tokens, or secret fields. **Depends on:** 3.1. **Acceptance:** Given each successful existing installation mutation, when it completes, then exactly one create/update/delete event has an ExecutorInstallation subject; given inline secret-like fields, then no secret is stored; given a rejected mutation, then no successful event is recorded. **Focused verification:** `cd control-plane && go test ./internal/executors ./internal/audit -run 'Test.*Audit|Test.*ExecutorInstallation'`.

- [ ] 3.4 Update `control-plane/internal/plans/runtime.go`, `runtime_test.go`, `control-plane/internal/workflow/plans.go`, `plans_test.go`, and worker wiring in `control-plane/cmd/api/main.go` so deterministic workflow code schedules an audit activity and the activity records existing PlanExecution/StepExecution start, complete, fail, and skip transitions; ApprovalRequest creation and decision; and eligible workflow-signal receipt. Use the workflow-engine actor, typed execution/request subjects, and safe status/decision diffs. **Depends on:** 2.2 and 2.3. **Acceptance:** Given an execution path with completed, failed, and skipped steps plus an approval signal, when the worker performs each transition, then exactly one corresponding audit event is recorded through the shared recorder; when a signal is received, then an audit activity—not workflow-side database I/O—records it. **Focused verification:** `cd control-plane && go test ./internal/plans ./internal/workflow ./internal/audit -run 'Test.*Audit|TestPlanWorkflow|TestPlanActivities'`.

## 4. Replace the frontend mock data source

- [ ] 4.1 Update `frontend/src/lib/rpc.ts`, rewrite `frontend/src/lib/audit/audit-store.ts`, and rewrite `frontend/src/lib/audit/audit-store.test.ts` as a generated-AuditService adapter. It must map proto events/filters to the canonical UI model, send the selected tenant ID, preserve server token order, and follow all server pages for existing CSV/JSON exports. **Depends on:** 1.2. **Acceptance:** Given a mocked AuditService response with a next token, when the browser loads or exports, then it uses the service response/token rather than local filtering or localStorage; given multiple pages, then export contains every returned matching event exactly once. **Focused verification:** `cd frontend && bunx vitest run src/lib/audit/audit-store.test.ts`.

- [ ] 4.2 Update `frontend/src/routes/admin/audit/+page.svelte` and add `frontend/src/routes/admin/audit/+page.test.ts`; add flat related-subject and event-type filter keys in both `frontend/src/lib/i18n/en.json` and `frontend/src/lib/i18n/pt-BR.json`. Delete `frontend/src/lib/mocks/audit-events.ts`, remove its mock append use from `frontend/src/lib/agent-catalog.ts`, and update any affected tests/imports. Keep the existing UI layout and CSV/JSON formats, but replace legacy task labeling with localized typed related-subject display and send filters server-side. **Depends on:** 4.1. **Acceptance:** Given either supported locale, when the page renders, then related-subject and event-type copy resolves through `translate()` in that locale; given a filter/page navigation, then the page calls the real adapter; no production import reads, seeds, appends, or resets mock audit events. **Focused verification:** `cd frontend && bunx vitest run src/lib/audit/audit-store.test.ts src/routes/admin/audit/+page.test.ts`.

## 5. End-to-end verification and delivery gates

- [ ] 5.1 Run migration, contract, and control-plane gates after all implementation tasks. **Depends on:** 1.1 through 3.4. **Acceptance:** Given the final migration and generated bindings, when validation and backend tests run, then Atlas validates the migration, Buf lint passes, and all Go tests pass. **Verification:** `atlas migrate validate --dir file://database/migrations`; `cd proto && buf lint`; `cd control-plane && go test ./...`.

- [ ] 5.2 Run frontend and generated-agent-runtime gates, reporting the documented frontend type-check baseline separately from any introduced error. **Depends on:** 1.2, 4.1, and 4.2. **Acceptance:** Given the final frontend and generated bindings, when lint/tests/type checks run, then frontend lint passes, audit focused tests pass, `bun run check` has no errors beyond the documented 12-error baseline, and Python lint passes. **Verification:** `cd frontend && bun run lint`; `cd frontend && bunx vitest run src/lib/audit/audit-store.test.ts src/routes/admin/audit/+page.test.ts`; `cd frontend && bun run check`; `cd agent-runtime && ruff check src/`.

- [ ] 5.3 Run the applicable application smoke check and OpenSpec validation. Helm lint is not applicable because this change modifies no Helm files. **Depends on:** 5.1 and 5.2. **Acceptance:** Given the migrated development stack, when API, worker, and frontend run, then a dev-auth PlanConfiguration update, executor-installation change, and workflow transition appear only in the selected tenant's audit page; when OpenSpec validation runs, then the change is strict-valid. **Verification:** `mise run dev`; `openspec validate audit-log-backend --strict --no-interactive`.
