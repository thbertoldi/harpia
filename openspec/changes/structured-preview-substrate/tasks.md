## 1. Contract and catalog foundation

- [ ] 1.1 Extend `proto/harpia/artifacts/v1/artifacts.proto` with `StructuredPreview` (`renderer_key`, `renderer_version`, `spec_json`, `assets`), `PreviewArtifactResponse.structured_preview = 8`, `ArtifactType.preview_renderer_key`, `ArtifactType.preview_renderer_version`, matching registration fields, and the `TabularData`/`TabularColumn`/`TabularRow`/semantic-type/`ChartSpec` payload messages. Preserve all existing preview `oneof` fields 1–7. Use a pinned `ArtifactRef` for ChartSpec tabular input and represent only scalar typed table cells. Regenerate checked-in Go, TypeScript, and Python consumers through `mise run buf-generate`; do not hand-edit generated files. **Depends on:** none. **Acceptance:** Given the regenerated clients, when a response contains `structured_preview`, then all clients expose its key, version, JSON bytes, and asset refs while the old markdown/image/LinkedIn variants remain readable; given an ArtifactType registration, when it declares a renderer, then the declaration round-trips. **Focused verification:** `mise run buf-generate && cd proto && buf lint`; `cd control-plane && go test ./internal/artifacts/...`; `cd frontend && bunx vitest run src/lib/artifacts/preview.test.ts`. **Whole-surface gates before completion:** `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error); `mise run acceptance`.

- [ ] 1.2 Add `database/migrations/000022_structured_preview_artifact_types.sql`; update `control-plane/internal/artifacts/{repository.go,seed.go,handler.go}`, `control-plane/internal/artifacts/{repository_test.go,handler_test.go,preview_handler_test.go}`, and any SQL test fixtures that select or insert `artifact_types`. Persist, select, upsert, and return the ArtifactType renderer key/version; seed every current type with either its clean registry renderer or an explicit legacy/no-projector declaration, and seed `harpia.artifacts.v1.TabularData` (`table.v1`) and `harpia.artifacts.v1.ChartSpec` (`chart.flint.v1`). Keep renderer declarations global catalog metadata; do not make them tenant settings. **Depends on:** 1.1. **Acceptance:** Given a fresh database, when the artifact seed runs, then the two new types and all renderer metadata are present; given an existing seeded type, when startup repeats, then its renderer declaration upserts idempotently; given a GetArtifactType request, then it returns the stored declaration. **Focused verification:** `cd control-plane && go test ./internal/artifacts/...`; `cd proto && buf lint`. **Whole-surface gates before completion:** `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error); `mise run acceptance`.

## 2. Tenant-safe control-plane projection

- [ ] 2.1 Refactor `control-plane/internal/artifacts/preview.go` into a platform-owned `PreviewProjector` interface/registry and add focused registry tests in `control-plane/internal/artifacts/preview_test.go` (or a new `preview_registry_test.go`). Update `control-plane/internal/artifacts/handler.go` and `preview_handler_test.go` so `PreviewArtifact` loads the ArtifactType and requested immutable ArtifactVersion under `identity.RequireTenant`, resolves only its declared registered projector, validates key/version/spec/assets before returning `structured_preview`, and otherwise uses the retained fixed switch followed by JSON fallback. Keep all registry composition in `control-plane/internal/artifacts/`; do not put provider SDKs or dynamic-plugin loading in a domain package. **Depends on:** 1.1, 1.2. **Acceptance:** Given a registered Table ArtifactType, when its version is previewed, then the response is a validated `table.v1` structured preview; given an unknown valid JSON ArtifactType, when no projector/legacy variant applies, then preview returns JSON rather than an unsupported-type error; given a projector output with a mismatched key/version, then preview rejects it. **Focused verification:** `cd control-plane && go test ./internal/artifacts/...`; `cd proto && buf lint`. **Whole-surface gates before completion:** `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error); `mise run acceptance`.

- [ ] 2.2 Add static and reference-aware payload validation in `control-plane/internal/artifacts/validation.go`; update the create/version paths in `control-plane/internal/artifacts/handler.go` and repository interface only as needed to resolve pinned references safely. Add or extend `control-plane/internal/artifacts/{validation_test.go,preview_handler_test.go,versioning_test.go}`. `TabularData` validation MUST enforce ordered unique typed columns, scalar values that match semantic types, row width, bounded preview rows/bytes, truncation/pagination consistency, source fingerprint, and RFC 3339 `as_of`. `ChartSpec` validation MUST require a hash-pinned `TabularData` ArtifactRef in the same tenant and validate every Flint field/encoding against that referenced schema; reject nested/executable cells, URL-backed data, functions, scripts, arbitrary renderer/backend options, stale hashes, and cross-tenant refs before storing an ArtifactVersion. **Depends on:** 1.1, 1.2. **Acceptance:** Given a valid typed tabular payload, when it is created, then it is persisted as an immutable ArtifactVersion; given a wrong-width/type row or invalid freshness metadata, then creation persists neither object nor version; given a ChartSpec whose referenced TabularData gains a later current version, then the ChartSpec continues to validate and use its original pinned version; given a cross-tenant or hash-mismatched ref, then it is denied without disclosure. **Focused verification:** `cd control-plane && go test ./internal/artifacts/...`; `cd proto && buf lint`. **Whole-surface gates before completion:** `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error); `mise run acceptance`.

- [ ] 2.3 Implement and register the `table.v1`, `chart.flint.v1`, `carousel.v1`, `markdown`, `image`, and `document.html` projectors in `control-plane/internal/artifacts/preview.go` (split into new focused `*_projector.go` files in that same package if clearer), with fixtures/tests in `control-plane/internal/artifacts/preview_test.go` and `preview_handler_test.go`. The chart projector MUST read only the ChartSpec's verified pinned `TabularData` version and emit inline data plus compact Flint semantics; asset refs MUST be tenant-resolved immutable refs. Normalize retained HTML shape fallback to `document.html` and ensure document source enters the sanitation/CSP path; do not return raw object-store locations, credentials, downstream endpoints, or foreign-tenant asset data. Migrate existing carousel, markdown, and image projectors only when their current preview data maps without loss; leave other legacy variants working. **Depends on:** 2.1, 2.2. **Acceptance:** Given a chart ArtifactVersion, when previewed after its tabular Artifact has changed, then emitted rows are from the pinned hash/version; given a carousel, markdown, or image Artifact, when previewed, then it emits its registered renderer key where migrated; given a document with active HTML or a foreign asset ref, then the unsafe content/ref is rejected or removed and no network-capable preview escapes. **Focused verification:** `cd control-plane && go test ./internal/artifacts/...`; `cd proto && buf lint`. **Whole-surface gates before completion:** `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error); `mise run acceptance`.

## 3. Allowlisted Svelte renderer registry

- [ ] 3.1 Add the lockfile-pinned `flint-chart@0.1.1` and `echarts` production dependencies in `frontend/package.json` and `frontend/bun.lock`. Refactor `frontend/src/lib/artifacts/preview.ts` and `preview.test.ts` to normalize fixed and structured responses, parse/validate JSON, and resolve only a finite renderer-key/version map. Create `frontend/src/lib/components/artifacts/renderers/{TablePreview.svelte,FlintChartPreview.svelte,CarouselPreview.svelte,DocumentPreview.svelte,MarkdownPreview.svelte,ImagePreview.svelte,JsonPreview.svelte}` plus `StructuredPreviewRenderer.svelte`; update `frontend/src/lib/components/ArtifactPreview.svelte` and `frontend/src/lib/components/artifacts/ArtifactPreviewSheet.svelte` to dispatch through the registry instead of a closed preview-kind UI switch. Use only Harpy Eclipse tokens, locked existing fonts, and opacity/transform/layout motion. **Depends on:** 1.1, 2.1, 2.3. **Acceptance:** Given every supported renderer key, when its valid structured preview arrives, then the corresponding local component renders; given an unknown key, version, malformed JSON, or component validation failure, then the sheet remains usable and shows JSON fallback; given an Artifact spec containing a module/URL string, then the frontend neither dynamically imports nor executes it. **Focused verification:** `cd frontend && bunx vitest run src/lib/artifacts/preview.test.ts`; `cd frontend && bun run check`. **Whole-surface gates before completion:** `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error); `mise run acceptance`.

- [ ] 3.2 Implement the accessible native table and local Flint/ECharts chart renderers in `frontend/src/lib/components/artifacts/renderers/{TablePreview.svelte,FlintChartPreview.svelte,StructuredPreviewRenderer.svelte}` with pure schema/adapter tests in `frontend/src/lib/artifacts/{preview.test.ts,preview-renderers.test.ts}`. `TablePreview` MUST use captioned semantic table markup, scoped headers, declared order, and expose truncation/pagination/freshness state. `FlintChartPreview` MUST accept only normalized inline semantic input, compile it via static `flint-chart` import to an ECharts option, block any data URL/script/function/backend-option input, have a localized accessible name, and present the identical pinned table through the table renderer as a non-visual equivalent; it MUST make no renderer/data network request. **Depends on:** 3.1. **Acceptance:** Given valid pinned table data, when rendered in both table and chart forms, then the column order and rows match; given truncated data, then localized metadata is present; given a chart spec containing `data.url`, a function, or an unsupported field, then Flint/ECharts is not called and JSON fallback appears. **Focused verification:** `cd frontend && bunx vitest run src/lib/artifacts/preview.test.ts src/lib/artifacts/preview-renderers.test.ts`; `cd frontend && bun run check`. **Whole-surface gates before completion:** `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error); `mise run acceptance`.

- [ ] 3.3 Implement `DocumentPreview.svelte` and its normalization/security helpers in `frontend/src/lib/artifacts/preview.ts`, with tests in `frontend/src/lib/artifacts/preview-renderers.test.ts`. Render only server-sanitized document HTML in a `srcdoc` iframe whose sandbox omits `allow-scripts` and whose document-level CSP defaults to `default-src 'none'`, blocks `connect-src`, `form-action`, `frame-src`, and `base-uri`, and permits only the minimum safe inline style/image policy. Test that scripts, event handlers, forms, meta refresh/base tags, embedded content, unsafe schemes, and external resource URLs are removed/blocked. Do not rely on iframe sandboxing alone. **Depends on:** 2.3, 3.1. **Acceptance:** Given hostile document markup, when it reaches the document renderer, then no active markup remains in `srcdoc`, no script sandbox permission exists, and CSP prevents network/form exfiltration; given safe static markup, then it renders without allowing a remote resource fetch. **Focused verification:** `cd frontend && bunx vitest run src/lib/artifacts/preview-renderers.test.ts`; `cd frontend && bun run check`. **Whole-surface gates before completion:** `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error); `mise run acceptance`.

- [ ] 3.4 Add all new table/chart/document accessibility and fallback copy as flat `translate()` keys to both `frontend/src/lib/i18n/en.json` and `frontend/src/lib/i18n/pt-BR.json`; cover the renderer registry and migrated markdown/image/carousel behavior in `frontend/src/lib/artifacts/preview.test.ts` and `preview-renderers.test.ts`. Do not add raw user-facing literals in renderer components. **Depends on:** 3.1, 3.2, 3.3. **Acceptance:** Given either supported locale, when a table is truncated, a chart opens its data equivalent, a document is constrained, or a fallback is displayed, then every visible label and aria-label resolves in English and pt-BR; given retained markdown/image/carousel variants, when normalized, then their prior preview behavior remains available through the registered renderer. **Focused verification:** `cd frontend && bunx vitest run src/lib/artifacts/preview.test.ts src/lib/artifacts/preview-renderers.test.ts src/lib/i18n`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error). **Whole-surface gates before completion:** `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error); `mise run acceptance`.

## 4. End-to-end acceptance and delivery evidence

- [ ] 4.1 Extend `control-plane/internal/acceptance/acceptance_test.go` (or add `control-plane/internal/acceptance/structured_preview_test.go`) with deterministic DB-backed scenarios for renderer declaration round-trip, tenant-scoped table/chart projection, ChartSpec pinned-version behavior after a newer table version exists, cross-tenant asset/reference denial, JSON fallback, and document sanitization/CSP projection data. The acceptance test MUST not call Odoo, store credentials, or require an ExecutorInstallation; it verifies the generic Artifact substrate only. **Depends on:** 1.1–3.4. **Acceptance:** Given the production artifact persistence path, when a tenant creates TabularData and a ChartSpec, then it receives a table/chart structured preview sourced only from its pinned version; given another tenant or hostile document content, then no foreign data or network-capable markup reaches the response. **Focused verification:** `mise run acceptance`; `cd control-plane && go test ./internal/acceptance -count=1`. **Whole-surface gates before completion:** `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint && bun run check` (record the documented 12-error baseline separately and fail on any new error); `mise run acceptance`.

- [ ] 4.2 Run and record the complete delivery gates after all implementation tasks, fixing failures before archive: `mise run buf-generate`; `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint`; `cd frontend && bunx vitest run src/lib/artifacts/preview.test.ts src/lib/artifacts/preview-renderers.test.ts src/lib/i18n`; `cd frontend && bun run check`; and `mise run acceptance`. Treat the frontend check's documented 12 errors as the baseline and report any new error separately. Helm lint is not applicable because this change does not modify `deploy/`; do not start `mise run dev` unless interactive runtime diagnosis becomes necessary. **Depends on:** 4.1. **Acceptance:** Given the completed implementation, when these commands run from the repository root/specified directories, then every applicable linter and test passes, frontend type errors do not exceed the documented 12-error baseline, and acceptance passes; otherwise the change remains unready and is not archived. **Focused verification:** exactly the commands in this task. **Whole-surface gates before completion:** exactly the commands in this task, including non-deferrable `mise run acceptance`.
