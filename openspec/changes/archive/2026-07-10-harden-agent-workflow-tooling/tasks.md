## 1. Effective and Least-Privilege OpenCode Roster

- [x] 1.1 Update `opencode.json` to set `default_agent: orchestrator`, main GLM and lightweight MiMo models, pin `@sveltejs/opencode`, disable built-in `build`/`plan`/`explore`/`general`, add deny-first reviewed Skill permissions, and centralize the shared Git-inspection and whole-surface gate allowlist. Given a pure project load, when configuration resolves, then Harpia roles are selected and unapproved skills/built-ins cannot bypass them. Verify with `jq empty opencode.json` and the roster validator from task 2.2.
- [x] 1.2 Update `.opencode/agent/{implementer,terra,scut}.md` to inherit the centralized bash policy while retaining only justified role-specific additions; preserve edit/task/GitHub/external-directory boundaries. Given any writer role, when permissions resolve, then its common safe commands equal the root policy and dangerous Git operations remain approval-gated or denied. Verify with `opencode debug agent implementer`, `opencode debug agent terra`, `opencode debug agent scut`, and task 2.2.
- [x] 1.3 Add `.opencode/agent/verifier.md` with a cheap model and exact fixed gate commands only; update `.opencode/agent/orchestrator.md` to allow and require `verifier` after writer self-checks. Given a requested standard gate, when verification runs, then it reports command/exit/raw evidence without editing, diagnosing, retrying altered commands, delegating, or accessing skills/GitHub/external paths. Verify with `opencode debug agent verifier`, `opencode debug agent orchestrator`, and task 2.2.

## 2. Version Pins and Deterministic Validation

- [x] 2.1 Update `.opencode/.gitignore`, `.opencode/package.json`, `.opencode/package-lock.json`, and `mise.toml` so package metadata is tracked, `opencode` is pinned to 1.17.18, OpenSpec is pinned to 1.5.0, and `@opencode-ai/plugin` plus its SDK lock entries equal the CLI version. Given any one-sided upgrade, when validation runs, then version drift is reported. Verify with `jq empty .opencode/package.json .opencode/package-lock.json`, `opencode --version`, and task 2.2.
- [x] 2.2 Add `scripts/ci/validate_opencode.ts` as a dependency-free Bun validator and `mise run opencode-validate`. Check raw JSON/TOML/YAML, canonical OpenSpec pointers/rule IDs, adapter presence, required agent files/modes/references, deny-first Skill policy, permission invariants, unsafe Git/GitHub allowances, stale aliases, exact package pins, `opencode debug config --pure`, and `opencode agent list --pure`; tolerate unrelated global roles and never print resolved configuration. Keep provider-catalog checks behind explicit `--models`. Given an invariant violation, when the validator runs, then it collects actionable failures and exits nonzero without requiring credentials.
- [x] 2.3 Add `scripts/ci/validate_opencode.test.ts` with isolated table-driven fixtures for a golden roster and failures covering parsing, defaults/built-ins, skills, permissions, dangerous Git, verifier exactness, role/model references, OpenSpec config, package drift, resolved CLI output, and the opt-in model path. Verify with `bun test scripts/ci/validate_opencode.test.ts` and `mise run opencode-validate`.

## 3. Canonical OpenSpec Workflow

- [x] 3.1 Version `.opencode/commands/opsx-{apply,archive,explore,propose,sync}.md` and `.opencode/skills/openspec-{apply-change,archive-change,explore,propose,sync-specs}/SKILL.md`; replace both archive adapters' nonexistent `general-purpose` sync target with `implementer`. Given a clean checkout, when OpenCode enumerates commands/skills, then all five workflows are available and archive delegation names an allowlisted subagent. Verify with `rg -n 'general-purpose' .opencode/commands .opencode/skills`, `git ls-files --error-unmatch` for one command and one skill after staging/commit, and task 2.2.
- [x] 3.2 Replace placeholder `openspec/config.yaml` with concise canonical pointers and valid rules for `proposal`, `specs`, `design`, and executor-cold `tasks`. Given artifact instructions, when OpenSpec resolves project policy, then AGENTS/Constitution context and task requirements are present without duplicated mutable architecture. Verify with `openspec instructions tasks --change harden-agent-workflow-tooling --json` and task 2.2.
- [x] 3.3 Add a tooling job to `.github/workflows/ci.yml` that installs exact OpenSpec 1.5.0 and OpenCode 1.17.18 packages, runs `openspec validate --all --strict --no-interactive`, validator tests, and the roster validator; add it to container-publication dependencies. Given an invalid active spec or roster invariant, when CI runs, then the tooling job fails and publishing cannot proceed. Validate workflow syntax by inspection and execute the same commands locally.

## 4. Integration and Evidence

- [x] 4.1 Run `openspec doctor`, `openspec validate --all --strict --no-interactive`, `bun test scripts/ci/validate_opencode.test.ts`, `mise run opencode-validate`, and `git diff --check`; fix every in-scope failure and record exact results. The authenticated `--models` extension is optional evidence and MUST NOT block deterministic completion when credentials/catalogs are unavailable.
- [x] 4.2 Inspect the resolved `orchestrator`, writers, reviewers, and `verifier`; confirm required roles load, disabled built-ins do not, the existing unrelated worktree is preserved, and only scoped Harpia/OpenSpec tooling files are staged or committed. Mark all tasks complete only after this evidence passes.
