## 1. Archive-delivery enforcement

- [x] 1.1 Implement `scripts/ci/openspec-delivery-check.sh` after reviewing all archived `tasks.md` and completion notes. The script must reject unchecked boxes, `agent-incapable`, unresolved `DEFERRED`, missing final `mise run acceptance`, and unsupported conditional/N-A evidence; it must identify every offending archive and exit non-zero. Given an archived violation, when the script runs, then it reports the archive and fails without rewriting history. Focused verification: `bash scripts/ci/openspec-delivery-check.sh`. Whole-surface gates: `mise run lint`, `openspec validate enforce-acceptance-before-archive --strict --no-interactive`.
- [x] 1.2 Add `openspec-delivery-check` and `acceptance` tasks to `mise.toml`, with acceptance running exactly `cd control-plane && go test ./internal/acceptance -count=1`. Given a developer invokes either mise task, when its underlying gate fails, then mise returns the failure. Depends on 1.1. Focused verification: `mise tasks | grep -E 'acceptance|openspec-delivery-check'`. Whole-surface gates: `mise run lint`, `openspec validate enforce-acceptance-before-archive --strict --no-interactive`.
- [x] 1.3 Update only the verification-gates section of `AGENTS.md`, `openspec/config.yaml`, `.opencode/skills/openspec-archive-change/SKILL.md`, and `.opencode/commands/opsx-archive.md`. Require final non-deferrable `mise run acceptance` tasks and hard-stop archival for incomplete or unverified work; replace the existing “do not block on warnings / archive after confirmation” behavior. Given an archive adapter sees incomplete work, when it checks the change, then it stops without a confirmation bypass. Depends on 1.1. Focused verification: `grep -nE 'acceptance|hard-stop|block archive|confirmation' AGENTS.md openspec/config.yaml .opencode/skills/openspec-archive-change/SKILL.md .opencode/commands/opsx-archive.md`. Whole-surface gates: `mise run lint`, `openspec validate enforce-acceptance-before-archive --strict --no-interactive`.

## 2. Production acceptance suite

- [x] 2.1 Create `control-plane/internal/acceptance/acceptance_test.go` with shared PostgreSQL setup that fails loudly when `HARPIA_TEST_DATABASE_URL` is missing or uses a superuser, documents the NOBYPASSRLS app-role SQL, and seeds real artifact types, ExecutorSKUs, and embedded PlanTemplates. Given an absent or superuser DSN, when acceptance starts, then the test fails rather than skipping. Focused verification: `cd control-plane && HARPIA_TEST_DATABASE_URL='' go test ./internal/acceptance -count=1`. Whole-surface gates: `mise run lint`, `cd control-plane && go test ./...`.
- [x] 2.2 In `control-plane/internal/acceptance/acceptance_test.go`, implement A1 through public production repositories/adapters: load the real embedded `linkedin-content-studio` template, materialize no-image/no-carousel values, validate RUNNABLE bindings without optional steps, then opt into images and prove validation blocks until a real compatible noop image ExecutorInstallation exists. Use deterministic LinkedIn `approval_only` installation config and real `plans.MaterializePlanConfiguration`/`BindingValidator` logic. Given opted-out optional capabilities, when RUNNABLE is validated, then optional bindings are not needed; given image opt-in without installation, then validation is blocked. Depends on 2.1. Focused verification: `cd control-plane && HARPIA_TEST_DATABASE_URL="$HARPIA_TEST_DATABASE_URL" go test ./internal/acceptance -run TestAcceptanceLinkedInOptionalCapabilities -count=1`. Whole-surface gates: `mise run lint`, `cd control-plane && go test ./...`.
- [x] 2.3 In `control-plane/internal/acceptance/acceptance_test.go`, implement A2 against the real `audit.Repository`: append a tenant-A event with a dedupe key and secret-like diff, prove tenant A lists one redacted event, tenant B cannot see it under forced RLS, and the duplicate is a no-op. Implement A3 through real noop image config validation and provider resolution; if full Temporal execution remains out of scope, add an explicit `t.Skip` TODO referencing `enforce-acceptance-before-archive`. Given the app role and noop configuration, when these adapters run, then RLS/dedupe/redaction and keyless noop resolution are proven without external calls. Depends on 2.1. Focused verification: `cd control-plane && HARPIA_TEST_DATABASE_URL="$HARPIA_TEST_DATABASE_URL" go test ./internal/acceptance -run 'TestAcceptance(Audit|NoopImage)' -count=1`. Whole-surface gates: `mise run lint`, `cd control-plane && go test ./...`.

## 3. CI publication gate

- [x] 3.1 Update `.github/workflows/ci.yml` with an `acceptance` job using pgvector Postgres 16, Atlas migrations, a NOBYPASSRLS non-superuser app role granted application table/sequence access, `HARPIA_TEST_DATABASE_URL` set to that role’s DSN, then `mise run acceptance` and `mise run openspec-delivery-check`. Make `build-containers` need acceptance in addition to existing gates. Given CI runs on a pull request or trunk, when acceptance/delivery hygiene fails, then container publication cannot start. Depends on 1.2 and 2.3. Focused verification: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` and `grep -n 'acceptance' .github/workflows/ci.yml`. Whole-surface gates: `mise run helm-lint`, `mise run lint`, `openspec validate enforce-acceptance-before-archive --strict --no-interactive`.

## 4. Final acceptance

- [x] 4.1 Run the non-deferrable delivery acceptance command `mise run acceptance` against the NOBYPASSRLS app-role database after migrations. Given the full acceptance suite, when it completes, then A1, A2, and A3 pass and any full-Temporal coverage is explicitly TODO-skipped with `enforce-acceptance-before-archive` named. Depends on 1.1 through 3.1. Focused verification: `mise run acceptance`. Whole-surface gates: `bash scripts/ci/openspec-delivery-check.sh`, `cd control-plane && go test ./...`, `cd frontend && bun run check`, `mise run lint`, `cd proto && buf lint`, `openspec validate enforce-acceptance-before-archive --strict --no-interactive`.
