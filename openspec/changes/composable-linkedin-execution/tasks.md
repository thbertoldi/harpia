## 1. Canonical documentation and reconciliation

- [x] 1.1 Amend `docs/architecture/harpia-platform.md` §§3, 4, 7.4, and 9 so a Conversation is lifecycle assistance over PlanConfigurations **and their PlanExecutions**, declared interaction checkpoints surface automatically, and Review/Revision is defined separately from Elicitation and Approval. **Depends on:** none. **Acceptance:** Given a reader of the Constitution, when they read the human-interaction glossary and §9, then they can distinguish missing-context elicitation, candidate review/revision, and irreversible-action approval, and see that execution checkpoints belong in the owning conversation. **Focused verification:** `git diff --check -- docs/architecture/harpia-platform.md`; `openspec validate composable-linkedin-execution --strict --no-interactive`.
- [x] 1.2 Reconcile unfinished LinkedIn scope in `openspec/changes/rich-linkedin-content/{proposal.md,design.md,tasks.md}` and `openspec/changes/agent-team-deferred-followups/{proposal.md,tasks.md,specs/conversational-configuration/spec.md}` by adding a dated forward reference to `composable-linkedin-execution`, marking only contradictory/unimplemented unified-post, branching, and text-only-publish tasks as superseded, and retaining completed historical evidence. **Depends on:** 1.1. **Acceptance:** Given an executor starting from either earlier change, when they inspect its unfinished LinkedIn work, then they are directed to this change rather than implementing incompatible branches or a separate unified-post design. **Focused verification:** `git diff --check -- openspec/changes/rich-linkedin-content openspec/changes/agent-team-deferred-followups`; `openspec validate composable-linkedin-execution --strict --no-interactive`.

## 2. Contracts, persistence, and artifact/version access

- [x] 2.1 Add `ArtifactRef`, `LinkedInPost`, and the immutable carousel-document reference fields to `proto/harpia/artifacts/v1/artifacts.proto`; register `LinkedInPost` and `LinkedInCarouselDocument` typed artifact validation in `control-plane/internal/artifacts/validation.go`; regenerate every checked-in consumer with `mise run buf-generate`. The carousel document ref MUST identify the tenant-scoped PDF derivative bytes and hash, while `LinkedInPost` MUST retain its text, optional `CarouselDraft`, and image refs. **Depends on:** 1.1. **Acceptance:** Given an authored post, when a carousel or image is enriched, then the same LinkedInPost Artifact id receives a new version-addressable ref; given a carousel candidate, then it contains a ref/hash for the exact PDF bytes that will be uploaded. **Focused verification:** `cd proto && buf lint`; `cd control-plane && go test ./internal/artifacts/...`; `cd frontend && bun run check` (record the documented 12-error baseline separately and fail on any new error).
- [ ] 2.2 Extend `proto/harpia/plans/v1/plans.proto` with PlanStep `HumanInteractionPolicy`, review enums/messages/RPCs, version-aware StepExecution output/input references, version-pinned ApprovalRequest fields, and review/approval request/response payloads; add review event kinds and payload contracts in `proto/harpia/chat/v1/chat.proto`; regenerate with `mise run buf-generate`. Preserve ElicitationRequest as the missing-context contract and do not overload its fields for review feedback. **Depends on:** 2.1. **Acceptance:** Given a content candidate, when review is raised, then its protocol names the exact ArtifactRef, StepExecution, PlanExecution, PlanStep, and overseer; given an approval request, then it names the exact final version/hash rather than only a mutable artifact id. **Focused verification:** `cd proto && buf lint`; `cd control-plane && go test ./internal/chat/... ./internal/plans/...`.
- [ ] 2.3 Add the next ordered migration under `database/migrations/` and update `database/migrations/atlas.sum` for `plan_review_requests`, RLS/forced RLS policy/indexes, review lifecycle data, version-pinned approval subject columns, and version-aware StepExecution ref columns. Update `control-plane/internal/plans/{repository.go,model.go}` (and their focused tests) to read/write these values tenant-safely. **Depends on:** 2.2. **Acceptance:** Given two tenants, when either reads, decides, or lists reviews/approvals, then it can access only its tenant's rows; given a review/revision, then the persisted subject and decision refs retain artifact id, version id, type key, and content hash. **Focused verification:** `cd database && atlas migrate hash --dir file://migrations`; `cd control-plane && go test ./internal/plans/...`; `mise run acceptance`.
- [ ] 2.4 Extend `control-plane/internal/artifacts/{repository.go,handler.go,preview.go,versioning_test.go,preview_handler_test.go}` and the executor artifact-store port in `control-plane/internal/executors/runtime/` so a caller can create/load a named ArtifactVersion, verify its content hash/type/tenant, and preview a pinned `LinkedInPost` version. Add a server-selected composite preview representation that renders text plus ordered carousel slides and optional images without adding the deferred Flint renderer substrate. **Depends on:** 2.1, 2.3. **Acceptance:** Given an approval that references an older post version, when preview or publishing loads it, then it receives that version rather than the Artifact's current version; given a text-only post, then the composite preview has no empty carousel/image section. **Focused verification:** `cd control-plane && go test ./internal/artifacts/... ./internal/executors/runtime/...`.

## 3. Template, active snapshot, and specialist contracts

- [ ] 3.1 Extend the YAML/template model and validation path in `control-plane/internal/plans/{template_seed.go,template_seed_test.go,repository.go,validation.go}` to persist/convert `human_interaction_policy`, reject optional middle steps unless `input_artifact_type == output_artifact_type`, and compute/store `active_step_keys` in `control-plane/internal/workflow.PlanExecutionSnapshot` when a PlanExecution is created. Do not derive an in-flight active graph from a later template/configuration edit. **Depends on:** 2.2, 2.3. **Acceptance:** Given a template whose optional middle step changes type, when the embedded template seeder validates it, then startup fails; given an execution created with images off, when configuration later enables images, then the existing snapshot still excludes `generate-image`. **Focused verification:** `cd control-plane && go test ./internal/plans/... ./internal/workflow/...`.
- [ ] 3.2 Re-author `control-plane/internal/plans/templates/linkedin-content-studio.yaml` as template version 3 with exactly `fetch-news → write-draft → author-content → draft-carousel → generate-image → publish`; make `author-content`, `draft-carousel`, and `generate-image` output `harpia.artifacts.v1.LinkedInPost`; retain carousel/image opt-in capabilities only on the two enrichment steps; and declare `WHEN_REQUIRED` elicitation plus `REQUIRED` review for every significant content step. Make `publish` the sole `LinkedInPost → PublishConfirmation` integration step with approval. Update catalog localization keys in `frontend/src/lib/i18n/{en,pt-BR}.json` for all changed template/step/input/checkpoint copy. **Depends on:** 3.1. **Acceptance:** Given the real embedded template, when it loads, then its only path to publish is linear and publish has one LinkedInPost input; given both enrichments opted in, then declared order is author → carousel → image → approval → publish. **Focused verification:** `cd control-plane && go test ./internal/plans/...`; `cd frontend && bunx vitest run src/lib/i18n`; `cd frontend && bun run check` (no errors beyond the documented 12-error baseline).
- [ ] 3.3 Update the LinkedIn content agent contracts and real runners in `agents/linkedin-content-specialist/0.1.0.yaml`, `agents/linkedin-carousel-senior/0.1.0.yaml`, `agent-runtime/src/harpia_agents/agents/{linkedin_content_specialist.py,linkedin_carousel.py,linkedin_voice.py}`, `agent-runtime/src/harpia_agents/{registry.py,worker.py}`, and their tests. The author agent MUST create a `LinkedInPost`; carousel/image enrichers MUST consume and return a new version candidate of that same post; the carousel specialist MUST return the existing missing-context elicitation result before drafting when context is insufficient and MUST accept review feedback on continuation. **Depends on:** 2.1, 2.2, 3.2. **Acceptance:** Given a carousel specialist with missing required context, when it runs, then it raises an elicitation rather than drafting unconditionally; given its answer and revision feedback, when it resumes, then the real specialist produces a revised LinkedInPost candidate. **Focused verification:** `cd agent-runtime && uv run pytest tests -k 'linkedin_content_specialist or linkedin_carousel'`; `cd agent-runtime && ruff check src/`.
- [ ] 3.4 Update the image executor at `control-plane/internal/executors/integrations/image/` and its runtime adapter tests so an opted-in image step reads/writes the versioned `LinkedInPost` composition and appends image ArtifactRefs without creating a separate downstream branch. Preserve provider/model/credential configuration exclusively on the image `ExecutorInstallation`. **Depends on:** 2.1, 3.2. **Acceptance:** Given image generation is enabled, when it completes, then its new post version retains accepted text/carousel and adds image refs; given it is disabled, then no image executor is called and the post aliases its upstream version. **Focused verification:** `cd control-plane && go test ./internal/executors/integrations/image/... ./internal/executors/runtime/...`.

## 4. Review, identity-skip, and pinned-approval workflow

- [ ] 4.1 Add tenant-safe ReviewRequest persistence, Connect handlers, authorization, watches, and Temporal signaling alongside the existing Human Interaction implementation in `control-plane/internal/plans/{handler.go,repository.go,runtime.go,elicitations_e2e_test.go}` and `control-plane/internal/workflow/client.go`. Add focused review handler/signal tests in new `control-plane/internal/plans/reviews_e2e_test.go`. **Depends on:** 2.2, 2.3. **Acceptance:** Given a pending review assigned to an overseer, when that overseer accepts or submits feedback, then exactly one persisted decision is authorized and its signal targets the request's execution/step; another tenant or user without the authorized role cannot decide it. **Focused verification:** `cd control-plane && go test ./internal/plans/...`.
- [ ] 4.2 Refactor `control-plane/internal/workflow/{plans.go,plans_test.go,worker.go}` to implement the declared per-content-step loop: run real executor; wait/resume elicitation when requested; record candidate ArtifactRef; create/wait for ReviewRequest; rerun on revision feedback; complete only on acceptance. Add `CreateReviewRequest`, `ResolveReviewRequest`, and review wait/signal activities to `PlanRuntimeStore` and worker registration. **Depends on:** 3.3, 4.1. **Acceptance:** Given the carousel step requests elicitation, when the answer signal arrives, then the real carousel executor is invoked again; given it then receives revise feedback, then it returns a new ArtifactVersion and a second review request; given accept, then the next step receives the accepted ref. **Focused verification:** `cd control-plane && go test ./internal/workflow/... ./internal/plans/...`.
- [ ] 4.3 Update the workflow skip/completion path in `control-plane/internal/workflow/{plans.go,plans_test.go}` and runtime persistence/event path in `control-plane/internal/plans/{runtime.go,runtime_test.go}`. For excluded identity-shaped enrichments, record a SKIPPED alias with input/output refs equal, resolve it downstream, suppress a visible active-graph row, and send version-aware `CompleteStepExecutionActivity` input. Fix the existing call near `plans.go` completion so `PlanExecutionID`, `PlanStepKey`, artifact id, version id, and hash are never omitted. **Depends on:** 3.1, 4.2. **Acceptance:** Given carousel is off, when its skip is processed, then publish receives the authored post ref through the alias; given any content step completes, then `STEP_BOUND` carries the correct execution id, step key, artifact id, version id, and hash. **Focused verification:** `cd control-plane && go test ./internal/workflow/... ./internal/plans/...`.
- [ ] 4.4 Update approval creation/resolution in `control-plane/internal/workflow/{plans.go,plans_test.go}` and `control-plane/internal/plans/{runtime.go,repository.go,approvals_handler.go}` so approval starts only after all active reviews accept, persists the final `LinkedInPost` ArtifactRef/hash, verifies it on decision, and invokes publish only with that ref. Add rejection/cancellation/stale-version regression cases. **Depends on:** 2.3, 4.2, 4.3. **Acceptance:** Given a carousel run reaches approval, when its card is raised, then it names the final composed post version/hash; given reject, cancel, or hash mismatch, then the LinkedIn publisher is called zero times; given approve, then it is called once with that pinned version. **Focused verification:** `cd control-plane && go test ./internal/workflow/... ./internal/plans/... ./internal/executors/integrations/linkedin/...`.

## 5. Real LinkedIn publication adapter

- [ ] 5.1 Add the narrow deterministic CarouselDraft-to-PDF derivative builder in `control-plane/internal/executors/integrations/linkedin/document.go` with tests in `document_test.go`; wire it through `handler.go` and the version-aware executor artifact-store port so the derived `LinkedInCarouselDocument` Artifact is tenant-scoped, immutable, and attached to the candidate LinkedInPost **before** its review/approval request. Do not introduce an editor, generic renderer registry, or Flint dependency. **Depends on:** 2.1, 2.4, 3.3. **Acceptance:** Given a carousel candidate, when the draft step completes, then the post version contains a PDF artifact ref/hash and the stored bytes are stable for identical structured input; given approval preview, then the structured carousel and the bytes to upload originate from the same pinned post version. **Focused verification:** `cd control-plane && go test ./internal/executors/integrations/linkedin/... ./internal/artifacts/...`.
- [ ] 5.2 Replace the text-only `LinkedInPostDraft` publish interface in `control-plane/internal/executors/integrations/linkedin/{handler.go,client.go,config.go}` with a version-pinned `LinkedInPost` request. Extend `client.go` to use real LinkedIn REST `/rest/posts` for text and the documented document sequence (`POST /rest/documents?action=initializeUpload` → PUT returned URL with pinned PDF bytes → `POST /rest/posts` document media) for carousels; retain OAuth/version/header/status handling in this adapter only. Extend `client_test.go`, `handler_test.go`, and `contract_test.go` with `httptest` request-order/body/byte assertions. **Depends on:** 4.4, 5.1. **Acceptance:** Given an approved text-only post, when the handler publishes, then one REST post request contains exactly the pinned text and no document calls; given an approved carousel, then exactly one initialize, one byte-identical PUT, and one document-post call occur; given reject/cancel, then none occur. **Focused verification:** `cd control-plane && go test ./internal/executors/integrations/linkedin/...`.

## 6. Execution conversation and version-pinned preview

- [ ] 6.1 Extend frontend generated contracts/client wrappers and execution view folding in `frontend/src/lib/{gen/harpia/**,plans/execution-view.ts,plans/execution-view.test.ts,plans/approvals.ts,plans/approval-card.ts,plans/elicitations.ts}` for version-aware `STEP_BOUND`, ReviewRequest events/actions, pinned approval subjects, and immutable execution active-step snapshots. Add `frontend/src/lib/plans/reviews.ts` and `reviews.test.ts` for review RPC behavior. **Depends on:** 2.2, 4.1, 4.3. **Acceptance:** Given an execution snapshot excludes images, when its events are folded, then no image row exists; given a review/approval event for a non-current version, then the view model preserves that version id and hash. **Focused verification:** `cd frontend && bunx vitest run src/lib/plans/execution-view.test.ts src/lib/plans/approvals.test.ts src/lib/plans/reviews.test.ts`; `cd frontend && bun run check` (report any errors above the documented 12-error baseline).
- [ ] 6.2 Add `frontend/src/lib/components/thread/ReviewRefCard.svelte` and focused tests; update `ApprovalRefCard.svelte`, `ElicitationRefCard.svelte`, `PlanExecutionCard.svelte`, `ThreadMessage.svelte`, the chat route that supplies execution snapshots, and `ArtifactPreview{,Sheet}.svelte` so review, approval, and preview open the exact pinned version, show one composed LinkedInPost/carousel subject, and render only the snapshot active graph. Use only Harpy Eclipse tokens and opacity/transform/layout motion. **Depends on:** 2.4, 4.4, 6.1. **Acceptance:** Given a pending carousel review or approval, when the user selects Preview, then the canonical sheet shows text plus carousel from the pinned version; given images are off, then the active card has no image row; given revision feedback is submitted, then the card settles and waits for the replacement candidate. **Focused verification:** `cd frontend && bunx vitest run src/lib/components/thread/PlanExecutionCard.test.ts src/lib/plans/execution-view.test.ts src/lib/artifacts/preview.test.ts`; `cd frontend && bun run check` (no errors above baseline).
- [ ] 6.3 Add every new user-facing review, revision, version-pin, execution-status, and changed LinkedIn Content Studio catalog string as flat keys in both `frontend/src/lib/i18n/en.json` and `frontend/src/lib/i18n/pt-BR.json`; update localization parity/hardcoded-copy tests that cover the changed cards. **Depends on:** 6.2. **Acceptance:** Given either supported locale, when review/approval/execution cards render, then every label, action, status, aria label, and catalog title resolves through a matching key with no raw user-facing literal. **Focused verification:** `cd frontend && bunx vitest run src/lib/i18n src/lib/components/thread`; `cd frontend && bun run lint`.

## 7. End-to-end acceptance and delivery gates

- [ ] 7.1 Extend `control-plane/internal/acceptance/acceptance_test.go` (and only required acceptance helpers in that package) with a production-adapter LinkedIn execution scenario covering the real embedded template: order fetch → write → author → optional carousel → optional image → approval → publish; no image executor/card row when images are off; carousel identity alias when off; real-specialist elicitation/resume; revision ArtifactVersion; pinned approval ref/hash; and publisher call counts/bytes for reject, cancel, text, and carousel paths. Use a local recording HTTP LinkedIn server for deterministic document upload; leave only a clearly named TODO-skip for live LinkedIn sandbox credentials. **Depends on:** 5.2, 6.3. **Acceptance:** Given the acceptance harness and an approved carousel, when `mise run acceptance` runs, then it proves one initialize/upload/document-post sequence with the approved PDF bytes; given reject or cancel, then it proves zero publisher calls. **Focused verification:** `mise run acceptance`.
- [ ] 7.2 Run and record every applicable whole-surface gate after all implementation tasks: `openspec validate composable-linkedin-execution --strict --no-interactive`; `cd proto && buf lint`; `cd control-plane && go test ./...`; `cd agent-runtime && ruff check src/`; `cd frontend && bun run lint`; `cd frontend && bun run check` (compare to the documented 12-error baseline and report any new errors separately); `mise run helm-lint`; and `mise run acceptance`. Do not mark this task complete if any applicable linter or acceptance command fails. **Depends on:** 7.1. **Acceptance:** Given the finished change, when all commands run on the complete tree, then every linter and acceptance command passes and frontend type checking introduces no error beyond the documented baseline. **Focused verification:** the exact commands in this task.
