# Claude Code entry

The shared, tool-neutral agent guide is the source of truth. Read it:

@AGENTS.md

Claude-specific notes:
- My `~/.claude` memory is a private recall aid only. Anything other agents
  (Codex, OpenCode, Cursor) must honor lives in `AGENTS.md` / `docs/` — never
  canonically in memory.
